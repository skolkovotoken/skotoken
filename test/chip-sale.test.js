const Token = artifacts.require("CHIPToken");
const Sale = artifacts.require("CHIPSale");
const Web3 = require("web3");
const web3 = new Web3();

contract("CHIPSale Tests", async (accounts) => {
    /* it("Checks the admin address", async () => {
        let instance = await Sale.deployed();
        const adminAddr = instance.adminAddr();
        assert.equal(adminAddr, accounts[1]);
    }) */
});